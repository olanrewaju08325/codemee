# CodeMe Academy System Settings Guide

## Dynamic Configurations
The `PlatformSetting` model in `models/system.py` acts as a KV store.

## Controls
Admins can dynamically alter:
- Enrollment windows (Open / Close dates).
- AI daily quotas per student.
- Certificate template defaults.
- Maintenance Mode toggles.

