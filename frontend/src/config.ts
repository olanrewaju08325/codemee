// Central frontend config (Part 4 AI Tutor scaffolding)
export const AI_CONFIG = {
  enabled: true,
  provider: 'mock',
  consentText:
    'CodeMe Assistant is an experimental tutor preview. It may give inaccurate guidance and never replaces instructor feedback. By using it, your questions are stored so the academy can improve the tutor.',
} as const
